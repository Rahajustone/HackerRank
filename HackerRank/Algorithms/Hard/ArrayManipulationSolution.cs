using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace HackerRank.Algorithms.Hard
{
    class ArrayManipulationSolution
    {
        // Complete the arrayManipulation function below.
        //static long ArrayManipulation(int n, int[][] queries)
        //{
        //    var a = new int[];

        //    var newArr = new int[n];

        //    for (int i = 0; i < queries.Length; i++)
        //    {
        //        int firstIndex = queries[i][0] - 1;
        //        int secondIndex = queries[i][1] - 1;
        //        int k = queries[i][2];

        //        newArr[firstIndex] += k;

        //        if (secondIndex+1 < n)
        //            newArr[secondIndex + 1] = k;
        //    }

        //    return newArr.Max();
        //}

        //public static int GetNumberOfIslands(int[,] binaryMatrix)
        //{
        //    for (int i = 0; i < binaryMatrix.Length; i++)
        //    {
        //        for (int j = 0; j < binaryMatrix.get; j++)
        //        {

        //        }

        //    }
        //}
    }
}
